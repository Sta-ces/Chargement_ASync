using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class LoadingAsync : MonoBehaviour
{

    /*public int m_sceneToLoad;
    public int m_sceneToUnload;

    private void OnTriggerEnter(Collider other)
    {
        if (m_sceneToLoad != 0)
            SceneManager.LoadSceneAsync(m_sceneToLoad, LoadSceneMode.Additive);
        if (m_sceneToUnload != 0)
            SceneManager.UnloadSceneAsync(m_sceneToUnload);
    }

    private void OnTriggerExit(Collider other)
    {
        int load = m_sceneToLoad;
        int unload = m_sceneToUnload;
        m_sceneToLoad = unload;
        m_sceneToUnload = load;
    }*/
}
